import React, {
	FunctionComponent,
} from 'react';


const {{pascalCase name}}: FunctionComponent = ({ children }) => {
	return (
		<>
			{ children }
		</>
	);
};

export default {{pascalCase name}};
